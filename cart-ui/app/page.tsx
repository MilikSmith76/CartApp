import type { JSX } from 'react';

const HomePage = (): JSX.Element => {
    return (
        <div>
            This page is only here because turbopack complains when it is
            missing.
        </div>
    );
};

export default HomePage;
